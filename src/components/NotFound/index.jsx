import React from 'react';
import './styles.scss';

NotFound.propTypes = {
    
};

function NotFound(props) {
    return (
        <div>
                 Not Found
        </div>
    );
}

export default NotFound;